# The gates.
pytest>=8.0
pytest-cov>=5.0
pytest-qt>=4.4
black>=24.0
flake8>=7.0
ruff>=0.6

# The delivery scripts. Nuitka compiles the application and wraps the setup
# program (buildexe.py, buildinstaller.py, builddmg.py); Pillow derives every
# icon from the one master (generate_icons.py). Neither is imported by the
# application itself, so neither belongs in requirements.txt.
Nuitka>=2.0
pillow>=10.0
